# Shared release templates

`release_templates.json` is the **canonical** built-in legal body catalog.

Copies:

- `app/src/main/assets/release_templates.json` (Android `TemplateCatalog`)
- `web/data/release_templates.json` (web companion)

When editing bodies, update this file and sync the copies (or regenerate via your usual process).
